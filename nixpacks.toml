[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = ["npm install --production"]

[phases.build]
cmds = ["echo 'Server ready - no build needed'"]

[start]
cmd = "node simple-server.js"
